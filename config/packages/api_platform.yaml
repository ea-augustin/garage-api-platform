api_platform:
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]
#        allow plain identifier id
    allow_plain_identifiers: true
    defaults:
        pagination_items_per_page: 15
